# CLAUDE.md

This repo's agent rules of engagement, project structure, coding conventions, domain rules, and `.agents/` documentation protocol all live in **[`AGENTS.md`](AGENTS.md)** at the repo root. They apply to every AI coding agent (GitHub Copilot, Codex, Cursor, Aider, Gemini, Claude, etc.) — there's nothing here that's Claude-specific.

Per-package coding patterns are in `packages/web/AGENTS.md`, `packages/api/AGENTS.md`, `packages/shared/AGENTS.md`, `packages/mcp/AGENTS.md`.

**Read `AGENTS.md` first and follow it exactly.**
